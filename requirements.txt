# Servidor Web y API
# fastapi/uvicorn se subieron desde 0.109.0/0.27.0: esa version usaba un
# starlette.TestClient incompatible con el httpx>=0.28 que exige google-genai
# (rompe con TypeError: Client.__init__() got an unexpected keyword argument
# 'app' -- validado contra la API real el 2026-07).
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==21.2.0
pydantic==2.12.5
pydantic-settings==2.7.1

# Cliente IA (Google Gemini) - SDK nuevo, requerido por File Search Store.
# 1.2.0 (pin original) NO tiene file_search_stores en absoluto -- validado
# contra la API real, es la version minima verificada que sí lo expone.
google-genai==2.14.0

# GCP: registro de tenants (Firestore) y bucket de contexto (GCS)
google-cloud-firestore==2.20.0
google-cloud-storage==2.19.0

# Cliente HTTP -- requerido por google-genai (>=0.28) y por el TestClient de FastAPI.
httpx==0.28.1

# Robustez y Utilidades
python-dotenv==1.0.1
